@inherits MeeYoo.Inventory.Web.RazorView<MeeYoo.Inventory.Messages.PickItem>

<form method="POST"  action="@Form.Action">
    @Form.LabelFor(c => c.Quantity)
    @Form.TextBoxFor(c => c.Quantity)
    <br />
    @Form.HiddenFor(c => c.WarehouseItemId, Model.WarehouseItemId)
    @Form.Submit()
</form>